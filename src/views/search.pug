extends base.pug
include mixins/video

block content 
    form(method="GET").search__form
        input(placeholder="Search by Title", type="text", name="keywords")
        button 
            i.fas.fa-search
    div.video__grid
        each video in searchVideos 
            +videoComponents(video)
        else 
            span.empty__message No Videos
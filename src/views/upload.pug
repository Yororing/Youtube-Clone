extend base

block content 
    h3 Upload my Video
    form(method="POST")
        input(placeholder="Title", required, type="text", name="videoTitle")
        input(type="submit", value="Upload Video")
extends ../base
include ../mixins/video 

block content
    div.profile__data
        img(src="/" + user.avatarUrl).avatar
        h3=user.username 
    div.video__grid
        each videos in user.videos 
            +videoComponents(videos)
        else 
            span.empty__message No Videos